export enum AppRoutes {
  collection = 'collection',
  listNft = 'list-nft',
  nftDetail = 'nft-detail',
  swap = 'swap',
  profile = 'profile'
}

export enum ProfileRoutes {
  activity = 'activity',
  orders = 'orders',
}
