interface NftOwner {
  address: string;
  endAddress?: string;
  readableAddress: string;
  accountRoute: string;
}

const useNftOwners = (nftAddress: string, tokenId: string) => {

};

export default useNftOwners;
